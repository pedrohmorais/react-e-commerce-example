import { colors } from 'theme';

const fontFamily = `
  font-family: "Roboto Condensed";
  font-size: 14px;
  line-height: 22px;
  color: ${colors.fontePadrao};
  letter-spacing: 1px;
`;
export default fontFamily;
