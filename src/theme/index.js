import colors from 'theme/colors';
import { typography, placeholder, fontWeight } from 'theme/typography';
import forms from 'theme/layout/forms';
import device from 'theme/layout/media-queries';
import pageContainer from 'theme/layout/pageContainer';
import cardContainer from 'theme/layout/cardContainer';
import flexbox from 'theme/layout/flexbox';
import fontFamily from 'theme/layout/fontFamily';

export {
  colors,
  cardContainer,
  device,
  flexbox,
  fontWeight,
  forms,
  pageContainer,
  placeholder,
  typography,
  fontFamily,
};
